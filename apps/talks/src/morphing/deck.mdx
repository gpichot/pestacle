---
author: Pestacle
title: Morphing Magic
description: A showcase of mesmerizing morph animations
---

layout: mainSection
transition: morph

---

import { Morph } from "@gpichot/spectacle-deck";

<Morph
  name="hero-word"
  style={{
    fontSize: "12rem",
    fontWeight: 900,
    letterSpacing: "-0.04em",
    lineHeight: 1,
    background: "linear-gradient(135deg, var(--text-base), var(--text-accent))",
    WebkitBackgroundClip: "text",
    WebkitTextFillColor: "transparent",
    textAlign: "center",
  }}
>
  MORPH
</Morph>

---

transition: morph

---

import { FadeIn } from "@gpichot/spectacle-deck";

<div style={{ display: "flex", alignItems: "center", gap: "4rem", width: "100%", padding: "0 4rem" }}>
  <Morph
    name="hero-word"
    style={{
      fontSize: "4rem",
      fontWeight: 900,
      letterSpacing: "-0.04em",
      lineHeight: 1,
      background: "linear-gradient(135deg, var(--text-base), var(--text-accent))",
      WebkitBackgroundClip: "text",
      WebkitTextFillColor: "transparent",
      flexShrink: 0,
    }}
  >
  MORPH
  </Morph>

  <div style={{ flex: 1 }}>
    <FadeIn delay={400} direction="right">
      <div style={{ fontSize: "1.8rem", lineHeight: 1.6, opacity: 0.9 }}>
        Elements glide, reshape, and transform between slides.
        No keyframes. No tweening code. Just give them the same name.
      </div>
    </FadeIn>
  </div>
</div>

---

layout: centered
transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "2rem" }}>

<Morph
  name="hero-word"
  style={{
    fontSize: "1.2rem",
    fontWeight: 700,
    letterSpacing: "0.3em",
    textTransform: "uppercase",
    color: "var(--text-accent)",
    opacity: 0.6,
  }}
>
  MORPH
</Morph>

<div style={{ fontSize: "3.5rem", fontWeight: 800, textAlign: "center", lineHeight: 1.2 }}>

The browser does the heavy lifting

</div>

<FadeIn delay={300} direction="up">
  <div
    style={{
      fontSize: "1.4rem",
      opacity: 0.7,
      textAlign: "center",
      maxWidth: "600px",
    }}
  >
    View Transitions API captures the old state, updates the DOM, then
    interpolates every visual property automatically.
  </div>
</FadeIn>

</div>

---

layout: section
section: "Act I — The List Weave"
transition: morph

---

transition: morph

---

<div style={{ display: "flex", flexDirection: "column", gap: "1.5rem", padding: "0 6rem" }}>

<Morph
  name="item-design"
  style={{
    background: "var(--bg-surface)",
    borderRadius: 16,
    padding: "1.5rem 2rem",
    color: "#fff",
    fontSize: "1.6rem",
    fontWeight: 700,
  }}
>
  Design
</Morph>

<Morph
  name="item-build"
  style={{
    background: "var(--text-accent)",
    borderRadius: 16,
    padding: "1.5rem 2rem",
    color: "#fff",
    fontSize: "1.6rem",
    fontWeight: 700,
  }}
>
  Build
</Morph>

<Morph
  name="item-ship"
  style={{
    background: "var(--bg-elevated)",
    borderRadius: 16,
    padding: "1.5rem 2rem",
    color: "#fff",
    fontSize: "1.6rem",
    fontWeight: 700,
  }}
>
  Ship
</Morph>

</div>

---

transition: morph

---

<div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: "2rem", padding: "0 3rem", height: "70%" }}>

<Morph
  name="item-design"
  style={{
    background: "var(--bg-surface)",
    borderRadius: 24,
    padding: "2.5rem",
    color: "#fff",
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
  }}
>
  <div style={{ fontSize: "2.5rem", fontWeight: 800 }}>Design</div>
  <div style={{ fontSize: "1.1rem", opacity: 0.85, lineHeight: 1.5 }}>
    Sketch ideas in MDX. Layouts snap into place. Your content drives the
    structure.
  </div>
  <div
    style={{
      fontSize: "4rem",
      opacity: 0.2,
      fontWeight: 900,
      textAlign: "right",
    }}
  >
    01
  </div>
</Morph>

<Morph
  name="item-build"
  style={{
    background: "var(--text-accent)",
    borderRadius: 24,
    padding: "2.5rem",
    color: "#fff",
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
  }}
>
  <div style={{ fontSize: "2.5rem", fontWeight: 800 }}>Build</div>
  <div style={{ fontSize: "1.1rem", opacity: 0.85, lineHeight: 1.5 }}>
    Components, themes, and animations compose together. Vite keeps it instant.
  </div>
  <div
    style={{
      fontSize: "4rem",
      opacity: 0.2,
      fontWeight: 900,
      textAlign: "right",
    }}
  >
    02
  </div>
</Morph>

<Morph
  name="item-ship"
  style={{
    background: "var(--bg-elevated)",
    borderRadius: 24,
    padding: "2.5rem",
    color: "#fff",
    display: "flex",
    flexDirection: "column",
    justifyContent: "space-between",
  }}
>
  <div style={{ fontSize: "2.5rem", fontWeight: 800 }}>Ship</div>
  <div style={{ fontSize: "1.1rem", opacity: 0.85, lineHeight: 1.5 }}>
    Static export. Deploy anywhere. Your audience sees a polished experience.
  </div>
  <div
    style={{
      fontSize: "4rem",
      opacity: 0.2,
      fontWeight: 900,
      textAlign: "right",
    }}
  >
    03
  </div>
</Morph>

</div>

---

layout: section
section: "Act II — Shape Shifting"
transition: morph

---

layout: centered
transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "2rem" }}>

<Morph
  name="shape"
  style={{
    width: 250,
    height: 250,
    borderRadius: "50%",
    background:
      "linear-gradient(135deg, var(--bg-surface), var(--text-accent))",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "2rem",
    fontWeight: 800,
  }}
>
  Circle
</Morph>

<Morph name="shape-label" style={{ fontSize: "1.2rem", opacity: 0.5 }}>
  A shape is just a starting point
</Morph>

</div>

---

layout: centered
transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: "2rem" }}>

<Morph
  name="shape"
  style={{
    width: 500,
    height: 120,
    borderRadius: 24,
    background: "linear-gradient(90deg, var(--text-accent), var(--bg-surface))",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "2rem",
    fontWeight: 800,
  }}
>
  Banner
</Morph>

<Morph name="shape-label" style={{ fontSize: "1.2rem", opacity: 0.5 }}>
  Same element, new identity
</Morph>

</div>

---

layout: centered
transition: morph

---

<div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: "3rem" }}>

<Morph
  name="shape"
  style={{
    width: 160,
    height: 160,
    borderRadius: 32,
    background: "linear-gradient(180deg, var(--bg-surface), transparent)",
    border: "4px solid var(--border)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "1.4rem",
    fontWeight: 800,
  }}
>
  Card
</Morph>

<div style={{ flex: "0 0 400px" }}>
  <Morph
    name="shape-label"
    style={{ fontSize: "2.5rem", fontWeight: 800, lineHeight: 1.3 }}
  >
    The same element became a circle, a banner, and now a card — all through
    morph.
  </Morph>
</div>

</div>

---

layout: section
section: "Act III — The Big Reveal"
transition: morph

---

transition: morph

---

<div style={{ display: "flex", alignItems: "center", justifyContent: "center", height: "100%" }}>

<Morph
  name="big-number"
  style={{
    fontSize: "20rem",
    fontWeight: 900,
    lineHeight: 1,
    color: "var(--text-base)",
    textAlign: "center",
  }}
>
  42
</Morph>

</div>

---

transition: morph

---

<div style={{ display: "flex", alignItems: "center", gap: "4rem", padding: "0 4rem" }}>

<Morph
  name="big-number"
  style={{
    fontSize: "8rem",
    fontWeight: 900,
    lineHeight: 1,
    color: "var(--text-base)",
    flexShrink: 0,
  }}
>
  42
</Morph>

<div style={{ display: "flex", flexDirection: "column", gap: "1.5rem" }}>
  <FadeIn delay={300} direction="right">
    <div style={{ fontSize: "2.5rem", fontWeight: 700 }}>
      The answer to everything
    </div>
  </FadeIn>
  <FadeIn delay={500} direction="right">
    <div style={{ fontSize: "1.3rem", opacity: 0.7, lineHeight: 1.6 }}>
      A number so large it dominates the screen — then gracefully steps aside to
      let the story unfold beside it.
    </div>
  </FadeIn>
</div>

</div>

---

transition: morph

---

import { AnimatedCounter } from "@gpichot/spectacle-deck";

<div style={{ display: "flex", alignItems: "flex-end", gap: "4rem", padding: "0 4rem" }}>

<div style={{ display: "flex", flexDirection: "column", gap: "1rem", flex: 1 }}>
  <div style={{ fontSize: "2rem", fontWeight: 700 }}>By the numbers</div>

  <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "1.5rem" }}>
    <div style={{ background: "var(--bg-surface)", borderRadius: 16, padding: "1.5rem", border: "1px solid var(--border)" }}>
      <div style={{ fontSize: "2.5rem", fontWeight: 800, color: "var(--text-accent)" }}>
        <AnimatedCounter to={9} duration={1500} />
      </div>
      <div style={{ opacity: 0.6 }}>Layouts</div>
    </div>
    <div style={{ background: "var(--bg-surface)", borderRadius: 16, padding: "1.5rem", border: "1px solid var(--border)" }}>
      <div style={{ fontSize: "2.5rem", fontWeight: 800, color: "var(--text-accent)" }}>
        <AnimatedCounter to={7} duration={1500} />
      </div>
      <div style={{ opacity: 0.6 }}>Animations</div>
    </div>
    <div style={{ background: "var(--bg-surface)", borderRadius: 16, padding: "1.5rem", border: "1px solid var(--border)" }}>
      <div style={{ fontSize: "2.5rem", fontWeight: 800, color: "var(--text-accent)" }}>
        <AnimatedCounter to={5} duration={1500} />
      </div>
      <div style={{ opacity: 0.6 }}>Themes</div>
    </div>
    <div style={{ background: "var(--bg-surface)", borderRadius: 16, padding: "1.5rem", border: "1px solid var(--border)" }}>
      <div style={{ fontSize: "2.5rem", fontWeight: 800, color: "var(--text-accent)" }}>
        <AnimatedCounter to={0} duration={1500} />
      </div>
      <div style={{ opacity: 0.6 }}>Config files needed</div>
    </div>
  </div>
</div>

<Morph
  name="big-number"
  style={{
    fontSize: "4rem",
    fontWeight: 900,
    lineHeight: 1,
    color: "var(--text-base)",
    opacity: 0.3,
    flexShrink: 0,
  }}
>
  42
</Morph>

</div>

---

layout: section
section: "Act IV — Converging Paths"
transition: morph

---

transition: morph

---

<div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "0 4rem", height: "100%" }}>

<Morph
  name="converge-a"
  style={{
    width: 140,
    height: 140,
    borderRadius: "50%",
    background: "var(--bg-surface)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "1.5rem",
    fontWeight: 800,
  }}
>
  MDX
</Morph>

<Morph
  name="converge-b"
  style={{
    width: 140,
    height: 140,
    borderRadius: "50%",
    background: "var(--text-accent)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "1.5rem",
    fontWeight: 800,
  }}
>
  React
</Morph>

<Morph
  name="converge-c"
  style={{
    width: 140,
    height: 140,
    borderRadius: "50%",
    background: "var(--bg-elevated)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "1.5rem",
    fontWeight: 800,
  }}
>
  Vite
</Morph>

</div>

---

transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "1.5rem", height: "100%" }}>

<div style={{ display: "flex", gap: "1rem", alignItems: "center" }}>

<Morph
  name="converge-a"
  style={{
    width: 80,
    height: 80,
    borderRadius: "50%",
    background: "var(--bg-surface)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "0.9rem",
    fontWeight: 800,
  }}
>
  MDX
</Morph>

<div style={{ fontSize: "2rem", opacity: 0.4 }}>+</div>

<Morph
  name="converge-b"
  style={{
    width: 80,
    height: 80,
    borderRadius: "50%",
    background: "var(--text-accent)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "0.9rem",
    fontWeight: 800,
  }}
>
  React
</Morph>

<div style={{ fontSize: "2rem", opacity: 0.4 }}>+</div>

<Morph
  name="converge-c"
  style={{
    width: 80,
    height: 80,
    borderRadius: "50%",
    background: "var(--bg-elevated)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff",
    fontSize: "0.9rem",
    fontWeight: 800,
  }}
>
  Vite
</Morph>

</div>

<Morph
  name="converge-result"
  style={{
    fontSize: "5rem",
    fontWeight: 900,
    background: "linear-gradient(135deg, var(--text-base), var(--text-accent))",
    WebkitBackgroundClip: "text",
    WebkitTextFillColor: "transparent",
  }}
>
  = Pestacle
</Morph>

</div>

---

transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "3rem", height: "100%" }}>

<Morph
  name="converge-result"
  style={{
    fontSize: "3rem",
    fontWeight: 900,
    background: "linear-gradient(135deg, var(--text-base), var(--text-accent))",
    WebkitBackgroundClip: "text",
    WebkitTextFillColor: "transparent",
  }}
>
  = Pestacle
</Morph>

<div style={{ display: "flex", gap: "2rem" }}>

<Morph
  name="converge-a"
  style={{
    width: 50,
    height: 50,
    borderRadius: "50%",
    background: "var(--bg-surface)",
    opacity: 0.3,
  }}
></Morph>

<Morph
  name="converge-b"
  style={{
    width: 50,
    height: 50,
    borderRadius: "50%",
    background: "var(--text-accent)",
    opacity: 0.3,
  }}
></Morph>

<Morph
  name="converge-c"
  style={{
    width: 50,
    height: 50,
    borderRadius: "50%",
    background: "var(--bg-elevated)",
    opacity: 0.3,
  }}
></Morph>

</div>

<FadeIn delay={400} direction="up">
  <div
    style={{
      fontSize: "1.4rem",
      opacity: 0.7,
      textAlign: "center",
      maxWidth: "600px",
      lineHeight: 1.6,
    }}
  >
    Three technologies converge, shrink into the background, and let the product
    speak for itself.
  </div>
</FadeIn>

</div>

---

layout: section
section: "Act V — Typography Dance"
transition: morph

---

transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "0.5rem", height: "100%" }}>

<Morph
  name="typo-fast"
  style={{ fontSize: "6rem", fontWeight: 900, color: "var(--text-base)" }}
>
  FAST
</Morph>

<Morph
  name="typo-fluid"
  style={{
    fontSize: "6rem",
    fontWeight: 200,
    fontStyle: "italic",
    color: "var(--text-accent)",
  }}
>
  FLUID
</Morph>

<Morph
  name="typo-fun"
  style={{
    fontSize: "6rem",
    fontWeight: 900,
    letterSpacing: "0.3em",
    color: "var(--text-muted)",
  }}
>
  FUN
</Morph>

</div>

---

transition: morph

---

<div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: "6rem", height: "100%", padding: "0 4rem" }}>

<div style={{ display: "flex", flexDirection: "column", gap: "2.5rem" }}>

<div style={{ display: "flex", alignItems: "center", gap: "1.5rem" }}>
  <Morph
    name="typo-fast"
    style={{
      fontSize: "2.5rem",
      fontWeight: 900,
      color: "var(--text-base)",
      flexShrink: 0,
    }}
  >
    FAST
  </Morph>
  <FadeIn delay={300} direction="right">
    <div style={{ fontSize: "1.1rem", opacity: 0.7 }}>
      Vite HMR keeps your iteration loop under 100ms
    </div>
  </FadeIn>
</div>

<div style={{ display: "flex", alignItems: "center", gap: "1.5rem" }}>
  <Morph
    name="typo-fluid"
    style={{
      fontSize: "2.5rem",
      fontWeight: 200,
      fontStyle: "italic",
      color: "var(--text-accent)",
      flexShrink: 0,
    }}
  >
    FLUID
  </Morph>
  <FadeIn delay={500} direction="right">
    <div style={{ fontSize: "1.1rem", opacity: 0.7 }}>
      View Transitions make every slide change feel cinematic
    </div>
  </FadeIn>
</div>

<div style={{ display: "flex", alignItems: "center", gap: "1.5rem" }}>
  <Morph
    name="typo-fun"
    style={{
      fontSize: "2.5rem",
      fontWeight: 900,
      letterSpacing: "0.3em",
      color: "var(--text-muted)",
      flexShrink: 0,
    }}
  >
    FUN
  </Morph>
  <FadeIn delay={700} direction="right">
    <div style={{ fontSize: "1.1rem", opacity: 0.7 }}>
      Write Markdown, sprinkle in React — presenting has never felt this good
    </div>
  </FadeIn>
</div>

</div>

</div>

---

layout: section
section: "Finale"
transition: morph

---

transition: morph

---

import { TypeWriter } from "@gpichot/spectacle-deck";

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "2rem", height: "100%" }}>

<Morph
  name="finale-text"
  style={{
    fontSize: "8rem",
    fontWeight: 900,
    lineHeight: 1,
    textAlign: "center",
    background:
      "linear-gradient(135deg, var(--text-base) 0%, var(--text-accent) 50%, var(--text-base) 100%)",
    backgroundSize: "200% 200%",
    WebkitBackgroundClip: "text",
    WebkitTextFillColor: "transparent",
  }}
>
  Your turn.
</Morph>

<FadeIn delay={500} direction="up">
  <TypeWriter speed={30} delay={800}>
    Every element on these slides used morph. Zero animation code written.
  </TypeWriter>
</FadeIn>

</div>

---

transition: morph

---

<div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "3rem", height: "100%" }}>

<Morph
  name="finale-text"
  style={{
    fontSize: "1.5rem",
    fontWeight: 700,
    letterSpacing: "0.2em",
    textTransform: "uppercase",
    opacity: 0.4,
  }}
>
  Your turn.
</Morph>

<div style={{ fontSize: "4rem", fontWeight: 800, textAlign: "center", lineHeight: 1.2 }}>

Give elements the same name.

The browser does the rest.

</div>

<FadeIn delay={400} direction="up">
  <div
    style={{ display: "flex", gap: "3rem", fontSize: "1.2rem", opacity: 0.5 }}
  >
    <span>transition: morph</span>
    <span>|</span>
    <span>{'<Morph name="...">'}</span>
    <span>|</span>
    <span>{'{ morph="..." }'}</span>
  </div>
</FadeIn>

</div>
