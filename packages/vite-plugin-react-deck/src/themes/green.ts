const colors = {
  primary: "#FFFFFF", // white — main text
  secondary: "#F49676", // coral — accent / highlights
  tertiary: "#A0C4BA", // muted teal-white — subtle text
};

const backgrounds = {
  primary: "#042F3B", // dark teal — main background
  secondary: "#063E4D", // slightly lighter — cards / surfaces
  tertiary: "#084D5F", // lighter still — alternate sections
};

export const themeTokens = {
  colors,
  backgrounds,
};
