// Dark theme
// Modern dark aesthetic with soft white text and vibrant accent

const colors = {
  primary: "#E8E8ED", // soft white — main text
  secondary: "#6E56CF", // vibrant purple — accent / highlights
  tertiary: "#9494A0", // muted gray — subtle text
};

const backgrounds = {
  primary: "#111113", // near-black — main background
  secondary: "#1C1C1F", // slightly lighter — cards / surfaces
  tertiary: "#252529", // lighter still — alternate sections
};

const fonts = {
  header: '"Inter", "Helvetica Neue", Helvetica, Arial, sans-serif',
  text: '"Inter", "Helvetica Neue", Helvetica, Arial, sans-serif',
};

export const themeTokens = {
  colors,
  backgrounds,
  fonts,
};
