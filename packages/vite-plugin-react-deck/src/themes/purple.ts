const colors = {
  primary: "#ffffff", // white — main text
  secondary: "#F530EC", // magenta — accent / highlights
  tertiary: "#B8A0C8", // muted lavender — subtle text
};

const backgrounds = {
  primary: "#2B135A", // dark purple — main background
  secondary: "#371A6E", // slightly lighter — cards / surfaces
  tertiary: "#432182", // lighter still — alternate sections
};

export const themeTokens = {
  colors,
  backgrounds,
};
