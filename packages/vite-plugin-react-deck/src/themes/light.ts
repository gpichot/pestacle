// Light theme
// Clean, minimal light theme with a whitish background

const colors = {
  primary: "#1a1a2e", // dark navy — main text
  secondary: "#0077b6", // vivid blue — accent / highlights
  tertiary: "#6b6b80", // muted gray — subtle text
};

const backgrounds = {
  primary: "#f5f5f7", // off-white — main background
  secondary: "#eaeaee", // slightly darker — cards / surfaces
  tertiary: "#e0e0e5", // darker still — alternate sections
};

const fonts = {
  header: '"Inter", "Helvetica Neue", Helvetica, Arial, sans-serif',
  text: '"Inter", "Helvetica Neue", Helvetica, Arial, sans-serif',
};

export const themeTokens = {
  colors,
  backgrounds,
  fonts,
};
